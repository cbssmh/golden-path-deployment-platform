# Integration tests

Runtime integration is performed by `make bootstrap`, `make verify`, and the
rebuild procedure in `runbooks/platform-rebuild.md`. It requires a reachable
Docker daemon, kind, and a public GitOps repository URL.
